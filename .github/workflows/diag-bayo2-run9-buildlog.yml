name: Cemu ARM64 Bayo2 Run9 Build Log Repro

on:
  push:
    branches:
      - diag-bayo2-run9-buildlog

concurrency:
  group: runtime-experiments-arm64
  cancel-in-progress: true

jobs:
  stop-stale-repro:
    runs-on: ubuntu-latest
    steps:
      - name: Stop stale repro
        run: echo "Run #9 cause and fix are already documented; no rebuild required."
